@AGENTS.md

<!-- The GitNexus block lives in AGENTS.md, which the import above pulls in.
     Keeping a second copy here loaded the same 40 lines twice per session.
     `gitnexus analyze` re-injects it if run bare -- always go through
     `just ai gitnexus sync`, which passes --skip-agents-md --skip-skills. -->
