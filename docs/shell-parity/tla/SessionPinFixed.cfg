\* Fixed resolution: empty cid on resend re-pins to THIS tty only (never
\* LAST_ACTIVE). Expect ALL invariants to hold (no counterexample).
SPECIFICATION Spec

CONSTANTS
  TTY   = {t1, t2}
  Cid   = {c1, c2, c3}
  Buggy = FALSE

INVARIANT TypeOK
INVARIANT INV_CID_NONEMPTY
INVARIANT INV_TTY_PIN
INVARIANT INV_CWD_FOLLOWS
