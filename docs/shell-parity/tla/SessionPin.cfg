\* Buggy resolution: empty cid on resend falls back to LAST_ACTIVE.
\* Expect INV_TTY_PIN to FAIL with a cross-tty leak counterexample.
SPECIFICATION Spec

CONSTANTS
  TTY   = {t1, t2}
  Cid   = {c1, c2, c3}
  Buggy = TRUE

INVARIANT TypeOK
INVARIANT INV_CID_NONEMPTY
INVARIANT INV_TTY_PIN
INVARIANT INV_CWD_FOLLOWS
