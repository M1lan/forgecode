\* Abort enabled: model the ZLE SIGINT between C and D. Expect INV-ABORT-PAIR
\* to FAIL with a reachable unpaired-B;C counterexample (documented known gap).
SPECIFICATION Spec

CONSTANTS
  MaxMarks   = 8
  AllowAbort = TRUE

INVARIANT TypeOK
INVARIANT INV_ABORT_PAIR
