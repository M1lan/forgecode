\* Buggy resolution, checking ONLY the PRIMARY invariant INV_TTY_PIN so TLC
\* surfaces the cross-tty LAST_ACTIVE leak trace (not the empty-cid case,
\* which INV_CID_NONEMPTY catches separately).
SPECIFICATION Spec

CONSTANTS
  TTY   = {t1, t2}
  Cid   = {c1, c2, c3}
  Buggy = TRUE

INVARIANT INV_TTY_PIN
